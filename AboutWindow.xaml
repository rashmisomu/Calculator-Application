<Window x:Class="CalculatorApp.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculatorApp"
        mc:Ignorable="d"
        Title="About Calculator" Height="230" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#FF343434"
        Foreground="White"
        ResizeMode="NoResize">

    <StackPanel Margin="8">
        <StackPanel Orientation="Horizontal">
            <Image Source="/myCalci.PNG" Width="50" Height="70"/>
            <TextBlock Text=" Calculator" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        </StackPanel>
        <TextBlock Text="This is a modern calculator app." TextWrapping="Wrap" Margin="0,0,0,10"/>
        <TextBlock Text="Version: 1.0.0" />
        <TextBlock Text="Developed by: S RASHMI" Margin="0,0,0,10"/>
        <Button Content="Close" Width="80" Margin="0,15,0,20" HorizontalAlignment="Right" Click="Close_Click" BorderBrush="DarkBlue" BorderThickness="2"/>
    </StackPanel>
</Window>
